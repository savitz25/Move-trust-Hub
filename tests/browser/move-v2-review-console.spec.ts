import{expect,test}from"@playwright/test";
test("reviewer inspects, filters, and records only a synthetic decision",async({page})=>{await page.goto("/experience-lab/v2/internal/review");const console=page.locator('[data-testid="review-console"]:visible').last();for(let attempt=0;attempt<3&&!await console.isVisible();attempt++){await page.waitForTimeout(3000);if(!await console.isVisible())await page.reload()}await expect(console).toBeVisible({timeout:15000});await expect(console.getByText("Open cases")).toBeVisible();await console.getByLabel("State filter").selectOption("WA");await expect(console.getByTestId("review-queue").getByText("WA",{exact:false}).first()).toBeVisible();await console.getByTestId("review-queue").locator("button").first().click();await expect(console.getByText("Side-by-side source evidence")).toBeVisible();await expect(console.getByText("STATE_REGULATOR")).toBeVisible();await console.getByRole("button",{name:"KEEP IN REVIEW"}).click();await expect(console.getByTestId("decision-history")).toContainText("KEEP IN REVIEW");await expect(console).not.toContainText("EXPERIMENTAL_DERIVED")});
