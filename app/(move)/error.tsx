'use client';

/**
 * Move hub segment errors share the same recovery UI + reporting as root.
 * Next requires a local error.tsx inside the segment for partial UI recovery.
 */
export { default } from '../error';
