/**
 * Homepage move entry — My Move Plan wizard (Plan → Build → Book).
 * Kept as HomeRouteFlow export so existing imports keep working.
 */
export { MyMovePlanWizard as HomeRouteFlow } from '@/components/my-move-plan/my-move-plan-wizard';
export type { HomeRouteMover } from '@/lib/home/resolve-route-from-zip';
