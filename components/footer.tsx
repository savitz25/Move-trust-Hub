/**
 * Legacy export — all Move layouts use HubSegmentShell → HubChrome → HubFooter.
 * Kept so any residual import still gets Ask Network seal + bar-compatible footer.
 */
export { HubFooter as Footer } from '@/components/hub/hub-footer';
