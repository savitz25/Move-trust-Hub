# Separate review pass

Method: self-review of the final diff, followed by the repository's automated PR review. No independent human review is claimed. Automated review status is recorded in the release receipt when available.

Reviewed task/name/identifier precedence, independent endpoints, role, recorded-headquarters versus route service, capability-level state grains, source clocks, URL/selection validation, publication/association projection, server-rendered forms, keyboard controls, noindex and normalized telemetry.

Corrections from review: preserve broadening fields across pagination; carry explicit role through existing identity evidence checks; reject incompatible endpoint-state refinements; do not label unsupported locality as an applied retrieval predicate; keep legal-suffix names outside the new locality classifier. The unchanged base already fails an unquoted `Movers in Motion LLC` case through its existing name/relational-clause parser. Quoted name research works and is protected; that separate name-parser limitation is not closed here.

The state registry is reused at capability level, not overall-state status. NJ enforcement availability does not establish a current roster. CA/TX/NY search-only roster capability does not establish zero movers. FL FDACS/IM is separate from FMCSA. No source updates, DB writes, profile publication or identity merges occurred. JK MC225850 remains a disputed stored observation and is not restored to trusted fields.

The parent move-ask-v1 consumer was inspected read-only. Existing modes/results/count/provenance remain compatible; journey metadata is additive. Guidance uses definition/fail_closed meaning with no provider observations. Actual provider research stays on the existing name/identifier executors, and explicit recorded-state research uses the existing directory/record paths.

Browser harness note: two initial broadening waits timed out despite rendered scoped records because a glob/full-page lifecycle wait did not complete. The final harness uses URL parameter checks at navigation commit followed by actual result locators. The complete local run passed 18 checks, no page errors, maximum core-case completion 2,007 ms. A screenshot is never substituted for successful navigation/action assertions.

Baseline limitations reproduced independently: 532 TypeScript diagnostics on exact base and candidate (no new normalized diagnostics); no configured lint (both prompt for setup; no configuration changed); county SEO 1,695 Tier-1 pages > unchanged cap 400 on both. Existing build type/lint ignore settings are inherited unchanged. The production build and relevant deterministic tests are reported separately; a build is not a typecheck pass.

Rollback: normal reviewed revert of this ticket only. No database rollback. Preserve R1-001/005/006 and the disputed-MC containment, including on rollback.

Final filter review also preserved natural-language current-authority conditions across consent, rejected conflicting typed authority, and retained unsupported route-count/ranking/price requests explicitly. An unavailable broader combination cannot claim that a cohort executed.

The structured V2 cohort adapter also had a directly connected city-to-state substitution. Its new pre-retrieval guard returns UNSUPPORTED_CAPABILITY with unchanged class/location, empty applied filters, and a native refinement destination. The original unsupported city was never a supported state cohort; no response shape or parent repository changed.
