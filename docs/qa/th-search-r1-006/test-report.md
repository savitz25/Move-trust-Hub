# Observed release certification

Runtime certified: `8ac3dc872a242dec48948d44db3c5cdb4a3502ad`, deployment `dpl_ENwVFLjNSj3SULSU8dJmV5KvnT5W`, canonical `www.movetrusthub.com` and `movetrusthub.com`. Production suite completed at 2026-09-12T01:10:16.357Z. The final documentation commit records an already deployed runtime; it does not fabricate its own future merge identity.

## Independent source and public outcome

| Request/surface | Observed final behavior |
|---|---|
| JK Moving Services licensed? | Distinct name candidates; selection retains JK's USDOT identity and source-name explanation, with MC under review |
| USDOT 1065394 | FOUND; trusted MC null; official company action uses USDOT1065394 |
| MC225850 | SOURCE_CONFLICT; JK excluded from legacy trusted results and displayed separately as a stored unconfirmed candidate |
| USDOT1065394 / MC225850 | Association not confirmed; no invalid-license or zero-mover conclusion |
| MC196957 | NO_MATCH in permitted effective index; independent official observation is not inserted as a stored exact match |
| USDOT1065394 / MC196957 | Clarification/index gap; no invented accepted pair |
| Public JK profile | MC under review, separate stored and official observations, USDOT verification links; no misleading 'MC not recorded' caption |
| SHIFL spaced USDOT / MC1019808; Allied MC15735 | Existing exact stored-source identities retained |
| USDOT3244 | Honest exact miss; no prefix match to3244649 |

The canonical row was read again after release: original MC225850, check time, update time and data hash are unchanged (`release-receipt.json`). The current official MC196957 observation is dated separately from retrieval. MC225850's holder and historical set remain unresolved. No production mutation or regulator-data refresh ran.

## Executed gates

| Gate | Actual result |
|---|---|
| Baseline discrepancy | Red on exact starting code; `red-before.log`, `before-production.json` |
| R1-006 | 14/14 pass, including actual cached-object projection and Verify action behavior |
| R1-001 | 17/17 pass |
| R1-005 | 15/15 pass |
| Related directory/identity/profile/customer/publication/schema | 85/85 pass |
| Repository `npm test` | Pass, including current state and metrics checks |
| Two mutations | Guard bypass and disputed company-link construction both fail; restored source passes |
| TypeScript | Fails: baseline535, candidate533, no new diagnostics after normalized comparison; not globally green |
| Lint | Unconfigured on exact base and candidate; not reported as passed |
| Optimized build | Local and normal Vercel builds pass with existing unchanged build configuration |
| County SEO circuit breaker | Both base and release fail1695 versus cap400; unrelated existing main-workflow failure, no cap change |

## Browser evidence and limitations

Final canonical production: 24 completed cases, zero page errors, no measured horizontal overflow at1280/390/320. Browser-plus-API case timings324–4619ms include optional-explanation waits; these are not pure server latency. Evidence includes homepage first Enter, name selection, Trace, reload, settled back/forward, conflict-to-miss edits without stale cards, profile links and responsive screenshots. Screenshots were visually inspected. NJ discovery/constrained count, definition and NY protection were included. `production-browser.json` records actual queries, returned fields, visible text and clocks.

The first production CDP session stalled after three completed cases; the incomplete artifact is retained and the complete suite was rerun in a fresh ticket-owned session. The first local history harness waited for a DOM event not guaranteed by SPA navigation; after reload it now waits for settled hydration, uses navigation commit, and asserts actual candidate/selected content. No history runtime change was made. The final local suite completed24 cases but reported one recoverable React418 hydration warning. The final production suite did not reproduce it; local browser output is not mislabeled error-free.

Interactive preview was login-protected and was not bypassed. Production Verify GET was checked; its existing POST writes an analytics record, so no production POST was issued. The actual compiled Verify action and UI were tested locally against source reads while the harness blocked all database writes; `local-verify.json` records the result. Fixture tests also assert that a known disputed MC cannot invoke external authority lookups.

`/api/companies` full and directory projections each returned HTTP200 for one bounded50-row page; JK was not on those pages, so those probes do not claim direct live JK field verification. The shared cache/mapper/DTO behavior is covered by executable tests, while live JK fields were verified through search, selected name result and public profile. No full-corpus download was performed for QA.

## Review, release and safe rollback

Separate self-review plus automated Vercel Agent reviews and normal CI; no independent human review. PR128 and PR129 merged without bypasses. The follow-up cache guard prevents a pre-release cached Company from reintroducing a trusted MC. A bounded final runtime-log window showed successful200/304 requests and no errors; this is not an assertion about all historic logs.

Consumer containment is released. Canonical correction is proposed separately in `canonical-correction-proposal.md`, not approved or executed. Any rollback must retain source-discrepancy containment and useful USDOT research; do not restore the old unqualified MC assertion. No database rollback is needed for this code release. R1-001/R1-005 remain closed; other search/source projects remain outside this certification.
