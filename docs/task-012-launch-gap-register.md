# Task 012 launch gap register

## P0 blockers

None identified. The launch must not proceed until every P1 gate below is closed, but the additive hybrid architecture provides a safe rollback path and no destructive prerequisite was found.

## P1 — required before launch

| Gap | Required closure |
|---|---|
| V2 consumer composition exists only under Preview | Render the evidence-first search modules at the existing `/` and `/local-movers` intents behind the documented Production cutover flag; preserve V1 copy/modules that are not replaced. |
| Production V2 read routes intentionally return 404 | Enable only sanitized, bounded read endpoints through a server-side launch flag; retain all write/internal routes as 404. Add rate limits before activation. |
| Provider identity migration incomplete | Review 33 exact-USDOT matches, 1 multi-identifier review, and 16 unmatched historical URLs. Preserve all 50 observed provider URLs until resolved; no fuzzy merge. |
| Full consumer database release is not published | Build an immutable full FL/WA release from the 86-provider evidence set. The eight-provider Task 011 operational proof must never become the complete public universe. |
| V1/V2 database read compatibility | Migration rehearsal logged legacy reads for `companies.usdot_status` against a schema where that column is absent. Define a compatibility view/adapter and prove provider pages without fallback errors; do not alter V1 destructively. |
| Homepage launch composition and navigation | Implement the approved information architecture: origin/destination entry, Research movers, Verify, Compare, Plan, Tools, Auto transport, Learn, and methodology; retain “We cite. You decide.” and no-paid-placement language. |
| Analytics action events | Preserve Move GA4 stream `G-433BDVV8MJ` and Vercel Analytics. Add privacy-bounded events for search/path choice, Trust Report/source open, compare, shortlist, calculators, Move Plan, website click, and phone click. Never send addresses, ZIP pairs joined to identity, inventory contents, email, phone, or exact move date. |
| Redirect/canonical rehearsal | Validate every redirect in one hop. Existing Next permanent redirects emit 308; decide at the edge whether the launch contract requires literal 301 or accepts permanent 308 semantics, and keep the matrix authoritative. |
| Sitemap release selection | Generate Production sitemaps only from the active approved release, while preserving existing state/county/editorial URLs. Exclude Preview, APIs, internal operations, redirects, and noindex pages. |
| Final cross-environment rehearsal | Run the Task 012 golden set against the candidate deployment with Production-like flags/host headers and rehearse database pointer rollback before approval. |

## P2 — safe post-launch

- Organization SSO/RBAC for the reviewer console; console stays Production-disabled until then.
- Central Move Plan redesign. Preserve `/my-move` and existing saved plans/bookmarks during transition.
- Dedicated MC verification UX beyond current DOT/FMCSA evidence.
- Full auto-transport discovery redesign; retain `/auto-transport` routes and current carrier/broker content.
- Separate portable/container identity model; preserve `/companies/pods` and `/companies/units-moving-portable-storage` without forcing HHG authority logic.
- Broader provider URL consolidation after identity review and Search Console observation.
- Performance work on slow Production provider/destination pages; do not combine it with URL migration.
- Expanded automated accessibility coverage beyond the launch smoke set.

## P3 — optional

- New visual design for preserved editorial pages.
- Additional sitemap segmentation beyond core/providers/local/guides/tools/auto when scale warrants it.
- Retirement of low-value legacy aliases after Search Console confirms equity transfer.
- New national SEO content. Task 012 intentionally creates none.
