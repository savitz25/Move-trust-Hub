import{performance}from"node:perf_hooks";import{compareProviderReports,getDiscoveryRelease,getProviderTrustReport,resolveOriginZip,searchLocalMovers}from"../../../lib/move-v2/consumer-discovery/server-read";
function bench(name:string,fn:()=>unknown,iterations=500){for(let i=0;i<25;i++)fn();const values:number[]=[];for(let i=0;i<iterations;i++){const start=performance.now();fn();values.push(performance.now()-start)}values.sort((a,b)=>a-b);return{name,iterations,p50Ms:Number(values[Math.floor(iterations*.5)].toFixed(3)),p95Ms:Number(values[Math.floor(iterations*.95)].toFixed(3)),maxMs:Number(values.at(-1)!.toFixed(3))}}
const release=getDiscoveryRelease(),ids=release.providers.slice(0,4).map(p=>p.providerId);console.log(JSON.stringify([bench("ZIP resolve",()=>resolveOriginZip("33401")),bench("local search",()=>searchLocalMovers({originZip:"33401"})),bench("interstate route",()=>searchLocalMovers({originZip:"33401",destinationZip:"98101"})),bench("Trust Report",()=>getProviderTrustReport(ids[0])),bench("compare 4",()=>compareProviderReports(ids))],null,2));
